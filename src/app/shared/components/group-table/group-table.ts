export interface IGroupTable {
  groupId: number; 
  description: string; 
  altern_GroupCode: string;
  parent_GroupCode: number,
  groupModule: string; 
  entityId: number;
  organizationId: number;
}