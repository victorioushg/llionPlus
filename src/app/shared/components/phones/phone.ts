export interface IPhone {
  phoneId: number;
  countryCode: string;
  phoneNumber: string;
  phoneType: string;
  entityId: number;
  organizationId: number;
}

export interface IPhoneType {
  type: string;
  name: string;
}
