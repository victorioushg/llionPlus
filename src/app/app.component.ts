import { Component } from '@angular/core';

@Component({
  selector: 'llion-root',
  template: '<router-outlet></router-outlet>',
  standalone: false, 
})
export class AppComponent {
  title = 'llion';
}
